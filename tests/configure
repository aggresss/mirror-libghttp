# configure file for test

PATH_REPO=`git rev-parse --show-toplevel`
PATH_TEST=${PATH_REPO}/test

cd ${PATH_REPO}/libghttp
./configure \
    --prefix=${PATH_TEST}/libghttp \
    --with-gcc
make
make install
